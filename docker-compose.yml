services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    environment:
      - DATA_DIR=/data
      - REPORT_DIR=/data/reports
    volumes:
      - backend_data:/data

volumes:
  backend_data:
